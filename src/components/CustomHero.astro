---
// CustomHero.astro — homepage hero (markup only).
// All styling lives in HomeStyles.astro (global, home-only) under .home-hero*.
interface Props {
  title?: string;
  tagline?: string;
  image?: string;
  imageAlt?: string;
  badge?: string;
  primaryLink?: string;
  primaryText?: string;
  secondaryLink?: string;
  secondaryText?: string;
}

// Locale-aware defaults so the hero also works on /en/ without explicit props.
const lang = Astro.url.pathname.startsWith("/en/") ? "en" : "da";
const defaults = lang === "en"
  ? {
      title: "Build your <span class='highlight'>smart home</span> with in-depth guides",
      tagline: "50+ in-depth guides for Home Assistant, ESP32 and automation — every one tested on a real setup.",
      badge: "New guides every week",
      imageAlt: "Illustration of a smart home: a Home Assistant dashboard with temperature, lighting and security controls.",
      primaryLink: "/en/home-assistant/kom-godt-i-gang/",
      primaryText: "Get started",
      secondaryLink: "/en/home-assistant/",
      secondaryText: "Browse all guides",
    }
  : {
      title: "Byg dit <span class='highlight'>smarte hjem</span> med danske guides",
      tagline: "50+ dybdegående guides til Home Assistant, ESP32 og automatisering — alle testet på en rigtig opsætning.",
      badge: "Nye guides hver uge",
      imageAlt: "Illustration af et smart hjem: et Home Assistant-dashboard med temperatur, lys og sikkerhed.",
      primaryLink: "/da/home-assistant/kom-godt-i-gang/",
      primaryText: "Kom i gang",
      secondaryLink: "/da/home-assistant/",
      secondaryText: "Se alle guides",
    };

const {
  title = defaults.title,
  tagline = defaults.tagline,
  image = "/images/hero/front.webp",
  imageAlt = defaults.imageAlt,
  badge = defaults.badge,
  primaryLink = defaults.primaryLink,
  primaryText = defaults.primaryText,
  secondaryLink = defaults.secondaryLink,
  secondaryText = defaults.secondaryText,
} = Astro.props;
---

<section class="home-hero" aria-labelledby="home-hero-title">
  <div class="home-hero__inner">
    <div class="home-hero__text home-reveal">
      <span class="home-hero__badge"><span class="dot" aria-hidden="true"></span>{badge}</span>
      <h1 id="home-hero-title" set:html={title} />
      <p class="home-hero__tagline">{tagline}</p>
      <div class="home-hero__cta">
        <a href={primaryLink} class="home-btn home-btn--primary">{primaryText}</a>
        <a href={secondaryLink} class="home-btn home-btn--ghost">{secondaryText}</a>
      </div>
    </div>
    <div class="home-hero__media home-reveal home-reveal-2">
      <div class="home-hero__frame">
        <img
          src={image}
          alt={imageAlt}
          width="1535"
          height="1023"
          loading="eager"
          fetchpriority="high"
          decoding="async"
        />
      </div>
      <div class="home-hero__stats" aria-hidden="true">
        <div class="home-hero__stat"><b>50+</b><span>Guides</span></div>
        <div class="home-hero__stat"><b>100%</b><span>Dansk</span></div>
        <div class="home-hero__stat"><b>0</b><span>Cloud</span></div>
      </div>
    </div>
  </div>
</section>
